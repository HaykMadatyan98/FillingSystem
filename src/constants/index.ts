export { ErrorMessages } from './error-messages'; // Exporting ErrorMessages directly
export { ResponseMessages } from './response-messages';

export enum ExpirationTimes {
  ACCESS_TOKEN = '1h',
  REFRESH_TOKEN = '24h',
}
